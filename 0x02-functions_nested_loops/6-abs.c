#include "main.h"

/**
*_abs- calculates te absolute value of an integer
*@n: this is an integer argumaent
*Return: this the absolute value of int
*/

int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	return (-n);
}
